## ## 5. Implementation Phases

### Phase 1: Authentication & Profiles
- [x] Connect Supabase Client.
- [x] Implement Auth flow (Sign up, Sign in, Sign out).
- [x] Logic: New signups get `user` role.
- [x] Profile setup: Address and phone number fields (implemented Full Name & comprehensive database fields).
- [x] Profile setup: Avatar image cropping and upload to secure Supabase storage buckets.
- [x] Dashboard shell (Sidebar + Sidebar Trigger).
- [x] Global Navigation Header with dynamic profile dropdown.
- [x] Protected route middleware and Admin access controls.

### Phase 2: Admin UI, Session & Category Management
- [x] CRUD for `categories`.
- [x] CRUD for `session_types`.
- [x] CRUD for `sessions`.
- [x] Implement Availability Rules using JSONB recurrence rules.
- [x] Admin UI for Session management (List, Create, Edit).
- [x] Layout UI: Collapsible Sidebar.
- [x] Layout UI: Small text breadcrumbs navigation in TopBar.
- [x] Dashboard UI: Trend Indicators on metric cards with date range filter.
- [x] Dashboard UI: Contextual Quick Actions.
- [x] Dashboard UI: Real-time Notification Center (Supabase Realtime).

### Phase 3: Booking Logic & Evolution
- [x] Implement Booking flow for clients.
- [x] Logic: First booking promotion (`user` -> `client`).
- [x] Constraint: Check `app_settings.max_booking_days_advance` before allowing a booking.
- [x] Admin Dashboard: Overview widgets (Total Bookings, Active Clients).

### Phase 4: History & Logging
- [x] Auth Hook: Save entry in `login_history` on successful login.
- [x] Database Triggers (or service logic) for `session_history`.
- [x] Master Booking history view for Admin.

### Phase 5: Admin Controls & Reporting
- [x] User Management: Deletion (Account removal) and Role management.
- [x] Reporting: Charts for session popularity (using Recharts).
- [x] Settings Page: Manage `app_settings` (inc. booking window).

### Phase 6: Polish & Testing
- [x] Top-right toast notifications for all feedback.
- [x] Skeleton loading states for dashboards.
- [x] Mobile optimization for dashboards.

### Phase 7: Sessions Tab Polish
- [x] Row clicks, persistent view (Eye) icons, details modal with client bookings list and delete/edit actions in Schedule, Types, and Categories tabs.

---

## ## 6. Functional Specs

### Role Promotion Logic
A Supabase Database Function triggered on `bookings` insert: